{% extends 'base.html.twig' %}

{% block title %}Légumes | {{ parent() }}{% endblock %}

{% block body %}


<div class="container mx-auto">
    <div class="grid grid-cols-4 gap-10 my-5">
        {% for vegetable in vegetables %}
            <div class="bg-gray-50 shadow-lg rounded border">
                <img src="{{vegetable.image}}" alt="{{vegetable.name}}" class="w-full h-48 object-cover rounded-t">
                <div class="p-4">
                    <h2 class="text-3xl font-black">{{vegetable.name}}</h2>
                    <p class="font-light my-4">{{vegetable.description}}</p>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
